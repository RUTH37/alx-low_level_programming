#include "main.h"

/**
 * *_strcpy - copies the string to by src, including \0.
 * @src: pointer.
 * @dest: pointer.
 * Return: the pointer to dest.
 */

char *_strcpy(char *dest, char *src)
{
	int i, lenght;

	for (lenght = 0; src[lenght] != '\0'; length++)
	{
	}

	for (i = 0; i <= length ; i++)
	{
		dest[i] = src[i];
	}
	return (dest);
}
